# AthenAI Lead Engine - runtime dependencies
#
# Versions are pinned with an upper bound on the major so `pip install -r
# requirements.txt` remains reproducible for months. The `>=` floors are the
# earliest versions we've actually exercised; the `<` ceilings are one major
# ahead. Update deliberately, not accidentally.

# Core (strict JSON schema validation + web layer)
pydantic>=2.7,<3
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
httpx>=0.27,<1

# Testing
pytest>=8.0,<10

# Verified working set (this is what the build was tested against):
#   pydantic 2.10.3, fastapi 0.115.5, httpx 0.28.1, uvicorn 0.32.1, pytest 9.1.1,
#   Python 3.13.5.
